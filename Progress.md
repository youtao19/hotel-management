# Progress

> 说明：这是“创建订单 -> 办理入住（单日/多日/休息房）”E2E 的修改流程；每完成一步我会同步勾选。

## 修改流程（需你确认后才开始写代码）

- [x] 1. 阅读 `业务说明.md`，确认下单/入住口径（单日/多日/休息房）与关键页面路径
- [x] 2. 盘点现有 Playwright 结构与一键命令（`npm run test:e2e`），确认测试基座可复用
- [ ] 3. 明确测试数据前置与可重复性：
  - 测试库中必须已有：房型、房间（可选：固定房号）、价格规则
  - 测试账号具备创建订单与办理入住权限
  - 每条用例都使用“唯一住客信息/手机号”等避免撞数据
- [ ] 4. 明确 UI 操作链路（以实际页面为准）：
  - 登录 -> 进入 `CreateOrder` 创建订单
  - 创建成功后获取订单（订单号/房号）并跳转到订单列表或房态页
  - 选择该订单执行“办理入住”，断言入住成功（状态变化/提示/按钮变化/房态变化）
- [ ] 5. 你确认测试样例设计（断言点）：
  - 用例 A：单日订单：入住日期=离店日期（或 1 天），创建 -> 办理入住成功
  - 用例 B：多日订单：跨 2 天及以上，创建 -> 办理入住成功
  - 用例 C：休息房订单：休息房类型/时段（按 UI 表单定义），创建 -> 办理入住成功
- [ ] 6. 实现通用辅助（Playwright fixture/helpers）：
  - 登录复用（可选：storageState）
  - 创建订单的表单填写封装（按订单类型参数化）
  - 在订单列表/房态页定位刚创建订单并触发入住
- [x] 6. 实现通用辅助（Playwright fixture/helpers）：
  - 创建订单表单填写封装（按订单类型参数化）
  - 订单列表搜索 -> 打开详情 -> 办理入住封装
- [x] 7. 新增 3 条 E2E 用例文件（同文件 3 case，串行执行）
- [x] 8. 本地执行 `npm run test:e2e`，稳定通过（5 passed）
- [ ] 9. 你最终确认用例与断言后：`git add -A`
- [ ] 10. 使用中文 commit message 执行 `git commit -m "..."`

## 测试样例（待你确认）

- 单日订单：创建订单（入住=今日，离店=明日）-> 查看订单搜索客人姓名 -> 订单详情“办理入住”-> 确认办理入住 -> 订单状态显示“已入住”
- 多日订单：创建订单（入住=今日，离店=后天）-> 同上办理入住并断言“已入住”
- 休息房订单：创建订单（入住=今日，离店=今日）-> 同上办理入住并断言“已入住”

## 我需要你补充确认的信息（否则第 5 步会卡住）

- [x] 单日订单/多日订单/休息房在 `CreateOrder` 页分别怎么选？（通过入住/离店日期组合）
- [x] 办理入住入口在哪里？（订单详情页，“办理入住”按钮/tooltip）
- [x] 是否允许测试用例直接创建新住客（姓名/手机号）？（允许）

## 已确认/已落地

- [x] 已选择登录态复用方案：Playwright `globalSetup + storageState`（登录一次生成 `playwright/.auth/state.json`，`npm run test:e2e` 已验证通过）

## 信息补充
- 办理入住按钮在订单详情页，按钮文案是“办理入住”，默认不显示文案，当把鼠标放上后显示文案。
- 允许测试用例直接创建新住客，姓名和手机号均可随机生成。
