# 修改进度（待你确认后开始执行）

## 目标拆解
1. 阅读 `业务说明.md`，确认收入统计与“详细收入数据”筛选的业务口径（不修改该文件）。
2. 评估 `backend/routes/revenueRoute.js` 现有 `daily/weekly/monthly` 路由是否冗余，并给出统一接口设计（时间范围参数）。
3. 重构后端收入统计接口：
   - 新增/调整为单一统计路由（`startDate`/`endDate`），返回总收入与必要的明细结构。
   - 直接修改前端适配新接口（不保留兼容路由）。
   - 删除 `overview` 路由，避免与 quick-stats 口径重复。
   - 将 `trend` 路由重命名为 `series`（更贴合“按时间聚合序列”的语义）。
4. 优化 `quick-stats`：
   - 将重复查询/重复计算收敛到更清晰的 service 层方法或单次 SQL 聚合查询。
   - 保证 Node.js 日期字段处理遵守规范（不对 DATE 用 `new Date()` / `toISOString()`）。
5. 前端“详细收入数据”表格行为调整：
   - 默认展示全部账单数据；
   - 支持按日期+房号筛选；
   - 日期默认选择“今日”（尽量把逻辑放到后端/接口层，前端只做参数传递与展示）。
6. 增加收入统计测试：先导入 `/sql/orders.csv` 数据，再执行测试，验证 2025-11-02~2025-11-07 每日收入是否与样例一致。
7. 你确认测试样例后，执行：`git add .` → 使用中文 commit message → `git commit -m "..."`。

## 当前状态
- [x] 1. 阅读业务说明并确认口径
- [x] 2. 分析现有路由冗余点与改造方案
- [x] 3. 实施后端路由/逻辑重构
- [x] 4. 优化 quick-stats 结构与查询
- [x] 5. 调整前端筛选默认行为
- [x] 6. 编写并运行收入统计测试（含导入 orders.csv）
- [ ] 7. 你确认后执行提交（git add + git commit）
