# 修复「收入统计页面卡片为 0」进度

## 修改流程（待确认）

1. **现状排查（已完成）**
   - 打开收入统计页面相关前端组件与接口调用代码，确认三个卡片的数据来源（接口/字段/计算逻辑）。
   - 对照后端返回结构/SQL 聚合逻辑，定位为何显示为 0（字段名不匹配、时间范围、单位、默认值、权限/门店过滤等）。

2. **根因修复（已完成）**
   - 在后端：修复统计接口的查询/聚合/过滤/返回字段（优先），确保返回值正确且字段命名稳定。
   - 在前端：仅做必要的映射与展示修正，尽量不增加额外业务判断。
   - 根据 `业务说明.md`：修复「详细收入数据表」默认展示全部账单；避免基础数据初始化因单接口失败而导致房型列表不渲染。
   - 修复：选择单日时「本周/本月」不应跟随所选日期，始终以数据库 `current_date` 为截止日统计。

3. **回归验证（未执行：你要求直接提交）**
   - 本地运行并访问收入统计页面，确认顶部三个卡片数值不再恒为 0。
   - 根据 `业务说明.md` 补充核对：每日营收明细表/房型分析/详细收入数据均能正常展示数据。
   - 后端 `npm test` 已通过（18 suites / 108 tests）。

4. **测试样例（未执行：你要求直接提交）**
   - 原计划：为统计接口增加最小可复现测试用例。

5. **提交代码（已完成）**
   - 已执行：`git add .`
   - 已执行：`git commit -m "修复收入统计页：单日不影响本周本月，明细默认展示"`
