## 收入统计页面
+ 在最上方的三个卡片中，第一个卡片用于显示单日收入，默认显示今日的收入，当我选择了一个单日日期后，这会个卡片显示这个日期的收入；另外两张卡片显示本周收入和本月收入。
+ 今日收入的逻辑：今日收入的金额=订单中，stay_date等于今天，且status不等于'cancelled'的订单的total_price的总和。
+ 本周和本月始终显示当前周和当前月的收入金额。
### 每日营收明细表
+ 根据选择的日期范围，房型，显示每日的营收明细。
### 详细收入数据表
+ 默认显示的是数据库中的所有账单数据。