# 日历房静态信息实施记录

## 已完成

1. 本地套餐新增 `douyin_business_type`，已有数据按 `PRESALE` 迁移。
2. 新增日历房规则表，保存有效期、取消规则、早餐、退款与上下架状态。
3. 新增本目录的校验、仓储、服务和路由；日历房不调用预售券业务模块。
4. 套餐页面支持选择日历房、维护规则和发起日历房同步。
5. 已补充请求体、业务互斥、规则校验和映射落库测试。

## 验收边界

本地测试、构建与静态检查只能证明本地行为。真实同步仍需要日历房权限、授权账号、已匹配物理房型及后端日志验证。
